# Copy to `.env.local` (gitignored) to point the dev server at a specific backend.
#
#   http://localhost:4000      default when unset — backend on this machine
#   http://192.168.1.20:4000   backend on the LAN, e.g. testing from a phone
#
VITE_API_URL=http://localhost:4000
